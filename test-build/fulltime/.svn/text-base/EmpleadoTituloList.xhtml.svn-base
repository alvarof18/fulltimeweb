<!DOCTYPE composition PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
    "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<ui:composition xmlns="http://www.w3.org/1999/xhtml"
    xmlns:s="http://jboss.com/products/seam/taglib"
    xmlns:ui="http://java.sun.com/jsf/facelets"
    xmlns:f="http://java.sun.com/jsf/core"
    xmlns:h="http://java.sun.com/jsf/html"
    xmlns:rich="http://richfaces.org/rich"
    template="#{requestScope.contextPath}/layout/template.xhtml">

<ui:define name="body">

    <h:form id="empleadoTituloSearch" styleClass="edit">

        <rich:simpleTogglePanel label="EmpleadoTitulo Search Filter" switchType="ajax">


        </rich:simpleTogglePanel>

        <div class="actionButtons">
            <h:commandButton id="search" value="Search" action="/fulltime/EmpleadoTituloList.xhtml"/>
            <s:button id="reset" value="Reset" includePageParams="false"/>
        </div>

    </h:form>

    <rich:panel>
        <f:facet name="header">EmpleadoTitulo Search Results</f:facet>
    <div class="results" id="empleadoTituloList">

    <h:outputText value="The empleadoTitulo search returned no results."
               rendered="#{empty empleadoTituloList.resultList}"/>

    <rich:dataTable id="empleadoTituloList"
                var="_empleadoTitulo"
              value="#{empleadoTituloList.resultList}"
           rendered="#{not empty empleadoTituloList.resultList}">
        <h:column>
            <f:facet name="header">
                <ui:include src="#{requestScope.contextPath}/layout/sort.xhtml">
                    <ui:param name="entityList" value="#{empleadoTituloList}"/>
                    <ui:param name="propertyLabel" value="Emti id"/>
                    <ui:param name="propertyPath" value="empleadoTitulo.emtiId"/>
                </ui:include>
            </f:facet>
            <h:outputText value="#{_empleadoTitulo.emtiId}"/>
        </h:column>
        <h:column>
            <f:facet name="header">
                <ui:include src="#{requestScope.contextPath}/layout/sort.xhtml">
                    <ui:param name="entityList" value="#{empleadoTituloList}"/>
                    <ui:param name="propertyLabel" value="Titulo titu id"/>
                    <ui:param name="propertyPath" value="empleadoTitulo.titulo.tituId"/>
                </ui:include>
            </f:facet>
            <h:outputText value="#{_empleadoTitulo.titulo.tituId}"/>
        </h:column>
        <h:column>
            <f:facet name="header">
                <ui:include src="#{requestScope.contextPath}/layout/sort.xhtml">
                    <ui:param name="entityList" value="#{empleadoTituloList}"/>
                    <ui:param name="propertyLabel" value="Detalle tipo parametro dtpa id"/>
                    <ui:param name="propertyPath" value="empleadoTitulo.detalleTipoParametro.dtpaId"/>
                </ui:include>
            </f:facet>
            <h:outputText value="#{_empleadoTitulo.detalleTipoParametro.dtpaId}"/>
        </h:column>
        <h:column>
            <f:facet name="header">
                <ui:include src="#{requestScope.contextPath}/layout/sort.xhtml">
                    <ui:param name="entityList" value="#{empleadoTituloList}"/>
                    <ui:param name="propertyLabel" value="Empleado empl id"/>
                    <ui:param name="propertyPath" value="empleadoTitulo.empleado.emplId"/>
                </ui:include>
            </f:facet>
            <h:outputText value="#{_empleadoTitulo.empleado.emplId}"/>
        </h:column>
        <rich:column styleClass="action">
            <f:facet name="header">Acci&#243;n</f:facet>
            <s:link view="/fulltime/#{empty from ? 'EmpleadoTitulo' : from}.xhtml"
                   value="#{empty from ? 'View' : 'Select'}"
             propagation="#{empty from ? 'none' : 'default'}"
                      id="empleadoTitulo">
                <f:param name="empleadoTituloEmtiId"
                        value="#{_empleadoTitulo.emtiId}"/>
            </s:link>
            #{' '}
            <s:link view="/fulltime/EmpleadoTituloEdit.xhtml"
                   value="Editar"
             propagation="none"
                      id="empleadoTituloEdit"
                rendered="#{empty from}">
                <f:param name="empleadoTituloEmtiId"
                        value="#{_empleadoTitulo.emtiId}"/>
            </s:link>
        </rich:column>
    </rich:dataTable>

    </div>
    </rich:panel>

    <div class="tableControl">

        <s:link view="/fulltime/EmpleadoTituloList.xhtml"
            rendered="#{empleadoTituloList.previousExists}"
               value="#{messages.left}#{messages.left} First Page"
                  id="firstPage">
          <f:param name="firstResult" value="0"/>
        </s:link>

        <s:link view="/fulltime/EmpleadoTituloList.xhtml"
            rendered="#{empleadoTituloList.previousExists}"
               value="#{messages.left} Previous Page"
                  id="previousPage">
            <f:param name="firstResult"
                    value="#{empleadoTituloList.previousFirstResult}"/>
        </s:link>

        <s:link view="/fulltime/EmpleadoTituloList.xhtml"
            rendered="#{empleadoTituloList.nextExists}"
               value="Next Page #{messages.right}"
                  id="nextPage">
            <f:param name="firstResult"
                    value="#{empleadoTituloList.nextFirstResult}"/>
        </s:link>

        <s:link view="/fulltime/EmpleadoTituloList.xhtml"
            rendered="#{empleadoTituloList.nextExists}"
               value="Last Page #{messages.right}#{messages.right}"
                  id="lastPage">
            <f:param name="firstResult"
                    value="#{empleadoTituloList.lastFirstResult}"/>
        </s:link>

    </div>

    <s:div styleClass="actionButtons" rendered="#{empty from}">
        <s:button view="/fulltime/EmpleadoTituloEdit.xhtml"
                    id="create"
           propagation="none"
                 value="Create empleadoTitulo">
            <f:param name="empleadoTituloEmtiId"/>
        </s:button>
    </s:div>

</ui:define>

</ui:composition>
